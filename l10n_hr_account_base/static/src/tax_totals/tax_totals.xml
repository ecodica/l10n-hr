<?xml version="1.0" encoding="utf-8"?>
<templates>

    <t t-name="l10n_hr_account_base.TaxGroupComponent" t-inherit-mode="extension" t-inherit="account.TaxGroupComponent">
        <!-- If storno modify amounts of tax groups -->
        <xpath expr="//t[@t-else='']/span[1]/t[@t-out='formatMonetary(props.taxGroup.tax_amount_currency)']" position="replace">
            <t t-if="props.totals.l10n_hr_is_storno">
                <t t-out="formatMonetary(-1*props.taxGroup.tax_amount_currency)" style="white-space: nowrap;"/>
            </t>
            <t t-else="">
                <t t-out="formatMonetary(props.taxGroup.tax_amount_currency)" style="white-space: nowrap;"/>
            </t>
        </xpath>
    </t>

    <t t-name="l10n_hr_account_base.TaxTotalsField" t-inherit-mode="extension" t-inherit="account.TaxTotalsField">
        <!-- If storno modify subtotals amount -->
        <xpath expr="//span[@t-att-name='subtotal.name']" position="replace">
            <t t-if="totals.l10n_hr_is_storno">
                <span t-att-name="subtotal.name"
                                  style="white-space: nowrap; font-weight: bold;"
                                  t-out="formatMonetary(-1*subtotal.base_amount_currency)"/>
            </t>
            <t t-else="">
                <span t-att-name="subtotal.name"
                                  style="white-space: nowrap; font-weight: bold;"
                                  t-out="formatMonetary(subtotal.base_amount_currency)"/>
            </t>
        </xpath>
        <!-- If storno modify total amount -->
        <xpath expr="//span[@name='amount_total']" position="replace">
            <t t-if="totals.l10n_hr_is_storno">
                <span
                    name="amount_total"
                    t-att-class="{'oe_subtotal_footer_separator': totals.has_tax_groups}"
                    t-out="formatMonetary(-1*totals.total_amount_currency)"
                    style="font-size: 1.3em; font-weight: bold; white-space: nowrap;"
                />
            </t>
            <t t-else="">
                <span
                    name="amount_total"
                    t-att-class="{'oe_subtotal_footer_separator': totals.has_tax_groups}"
                    t-out="formatMonetary(totals.total_amount_currency)"
                    style="font-size: 1.3em; font-weight: bold; white-space: nowrap;"
                />
            </t>
        </xpath>
    </t>
</templates>
